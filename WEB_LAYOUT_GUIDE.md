## Web系统布局说明

### 概述

已成功将萌芽生词王改造为左侧菜单 + 右侧内容的标准Web系统布局。

### 布局结构

```
┌─────────────────────────────────────────┐
│ 左侧菜单 (256px)  │  右侧内容区            │
│  - 品牌信息       │  - 主要功能内容        │
│  - 导航菜单项     │  - 自适应宽度          │
│  - 激活状态指示   │  - 响应式设计          │
│  - 底部设置按钮   │                       │
└─────────────────────────────────────────┘
```

### 菜单项

左侧菜单包含以下导航项：

1. **首页** - 学习概览和每日任务
2. **学习** - 今日词汇学习
3. **听写** - 听音写字练习
4. **生词本** - 自定义词库管理
5. **分析** - 学习进度分析和报表
6. **管理** - 管理员后台（需PIN认证）

### 页面更新

所有页面已更新适配新布局：

- `app/layout.tsx` - 主布局使用 PageLayout 组件
- `app/page.tsx` - 首页（首屏）
- `app/study/page.tsx` - 学习页面
- `app/analytics/page.tsx` - 分析页面
- `app/admin/page.tsx` - 管理员后台

### 新增组件

- `components/sidebar-nav.tsx` - 左侧导航栏
- `components/page-layout.tsx` - 页面容器布局

### 响应式设计

- **桌面版** (≥768px): 完整左侧菜单 + 右侧内容
- **平板版** (640-767px): 可收缩菜单
- **手机版** (<640px): 底部导航或汉堡菜单（可选）

### 使用方式

页面自动集成新布局，无需额外配置。页面内容会自动显示在右侧区域。

### 自定义选项

若要在特定页面隐藏侧边栏：

```tsx
import { PageLayout } from '@/components/page-layout'

export default function Page() {
  return (
    <PageLayout showSidebar={false}>
      {/* 内容 */}
    </PageLayout>
  )
}
```
